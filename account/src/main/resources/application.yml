server:
  port: 8080
spring:
  datasource:
    url: jdbc:h2:mem:test
    driver-class-name: org.h2.Driver
    username: sa
    password: ''
  h2:
    console:
      enabled: true
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
    show-sql: true
  config:
    import:
      - "application_qa.yml"
      - "application_prod.yml"
  profiles:
    active:
      - "qa"

build:
  version: "3.0"

accounts:
  message: "Welcome to Bank Acounts related Apis"
  contactDetails:
    name: "hrishi"
    email: "hrishikeshshedge@gmail.com"
  onCallSupport:
    - (555)555-333
    - (555)555-112

  application:
    name: account